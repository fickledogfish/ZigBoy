pub usingnamespace @import("sharp_lr35902.zig");
pub usingnamespace @import("sharp_lh5264n4.zig");

const PPU_CLOCK: usize = 4 * 1024 * 1024; // 4 MHz

const SCREEN_RESOLUTION = .{
    .x = 160,
    .y = 144,
};
